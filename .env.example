# X API – OAuth 1.0a (user context). Copy to .env and fill in.
# All four values must be from the SAME developer app (see docs/CREDENTIALS.md).
# Get them from developer.x.com → your app → Keys and tokens.
# Access Token and Secret: generate under "Access Token" for your user (same app).

TWITTER_API_KEY=your_consumer_key
TWITTER_API_SECRET=your_consumer_secret
TWITTER_ACCESS_TOKEN=your_access_token
TWITTER_ACCESS_SECRET=your_access_token_secret
