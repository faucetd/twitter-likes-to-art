# Core (parse + download + rename)
requests>=2.28.0

# API source (--source api): OAuth 1.0a for X API v2
requests-oauthlib>=1.3.0

# Tweet lookup via internal GraphQL API (no paid API needed)
twikit>=2.3.0

# Scrape fallback: gallery-dl for cookie-based media download
gallery-dl>=1.26.0

# Optional: art filter (CLIP)
# Uncomment to enable --filter-art in run.py
# open-clip-torch>=2.20.0
# torch>=2.0.0
# Pillow>=9.0.0

# Web app (wall peepo)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
Pillow>=9.0.0

# Deduplication
ImageHash>=4.3.0
