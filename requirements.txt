# Core (parse + download + rename)
requests>=2.28.0

# API source (--source api): OAuth 1.0a for X API v2
requests-oauthlib>=1.3.0

# Optional: art filter (CLIP)
# Uncomment to enable --filter-art in run.py
# open-clip-torch>=2.20.0
# torch>=2.0.0
# Pillow>=9.0.0
